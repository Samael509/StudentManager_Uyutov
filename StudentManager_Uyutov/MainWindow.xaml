<Window x:Class="StudentManager_Uyutov.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudentManager_Uyutov"
        mc:Ignorable="d"
        Title="SQLite Demo" Height="350" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal"
                    Margin="0 0 0 10" Grid.Row="0">
            <TextBox x:Name="InputName" Width="200"
                     Margin="0 0 10 0"/>
            <Button Content="Добавить" Width="100" Click="Add_Click"/>
        </StackPanel>
        <Button Content="Удалить выбаноое" Width="150"
        Grid.Row="1" Margin="0 0 10 10" Click="Delete_Click"/>
        <DataGrid x:Name="DataGridPeople" Grid.Row="2" SelectionChanged="DataGridPeople_SelectionChanged"/>

    </Grid>
</Window>
