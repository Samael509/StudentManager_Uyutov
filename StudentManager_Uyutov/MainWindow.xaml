<Window x:Class="StudentManager_Uyutov.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Student Manager" Height="400" Width="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" >
            <TextBox x:Name="InputName" Width="250" Margin="0,0,10,0" Padding="5" />
            <Button Content="Добавить" Width="120" Background="#4CAF50" Foreground="White" FontWeight="Bold" Click="Add_Click"/>
        </StackPanel>

        <Button Content="Удалить выбранное" Width="150" Margin="0,0,0,10" Grid.Row="1"
                HorizontalAlignment="Left" Background="#F44336" Foreground="White" FontWeight="Bold" Click="Delete_Click"/>

        <DataGrid x:Name="DataGridPeople" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" LoadingRow="DataGrid_LoadingRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>